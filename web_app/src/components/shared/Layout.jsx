import React from 'react';

const Layout = ({ children }) => {
  return (
    <div>
      {/* You can add header, footer, or navigation here */}
      {children}
    </div>
  );
};

export default Layout;
